# Native client (replace the Tauri/WebView2 frontend)

Decision (user, 2026-09-23): move the desktop client off the web stack. The daemon, tracer and `crates/protocol` stay as they are; only `apps/tauri-app` is replaced. Chosen stack: GPUI (Zed's UI framework) + `alacritty_terminal`, with Iced as the fallback if GPUI's API churn becomes a problem. The native client can run side by side with the Tauri app against the same daemon (it accepts multiple clients), so parity is reached feature by feature before Tauri is retired.

Rulings (user, 2026-09-23):

- **Crate:** the client is a main-workspace member at `apps/native`, under the workspace lints, prek clippy/test and `cargo deny check`.
- **Tauri freeze:** `apps/tauri-app` takes bug fixes only. Every new feature goes to the native client, so the parity checklist stops growing. Tauri is deleted at cutover (Phase 6).
- **Landing:** each item is committed to `main` as soon as it is green, and pushed at checkpoints (a finished item group or phase), not after every item (user, 2026-09-25).
- **Autonomy (user, 2026-09-25):** drive the native client to done without stopping between slices or phases. Technical decisions are settled in the session and recorded here as rulings. Only questions about behaviour the user will see go to the user. Later phases are split into items the same way.

What each feature must do is in [native-client-parity.md](./native-client-parity.md): tick its lines there as native items land.

## Spike

The spike (a standalone cargo workspace, now removed) became `apps/native` in P1.1. It reads `daemon.json`, connects, sends `Hello`, picks a session, loads its scrollback, then streams `PtyOutput` into `alacritty_terminal` and paints the grid with GPUI. Keys, paste (Ctrl+Shift+V, bracketed when the child asks) and wheel scrollback work.

Known limits carried into `apps/native` until the Phase 1 items replace them: it sends `Resize`, so the attached session's PTY follows this window's size; terminal replies (`PtyWrite`) are dropped; no selection/copy, no IME, no link detection; wide glyphs are not verified against CJK/emoji.

- [x] Spike: GPUI window attaches to a daemon session and renders it with `alacritty_terminal`
- [x] Hand-test the spike against a plain-shell session and against `claude`
- [x] Inventory the Tauri app's features into a parity checklist
- [x] Split the parity checklist into migration phases (below)

## Phase 1 — daily driver

Goal: the native client replaces the Tauri app for everyday work on the local machine: connect, see sessions, spawn, and work in tabs of split terminal panes. Items are in dependency order; P1.1 comes first, and P1.2 comes before P1.3.

- [x] **P1.1 Crate.** Create `apps/native` (binary `rustling-tulip-native`) as a workspace member from the spike's code: GPUI 0.2.2, `alacritty_terminal` 0.26, `crates/protocol`. `.\rt.ps1 clippy`, `cargo test --workspace` and `cargo deny check` pass: add any licence GPUI's tree needs to `deny.toml` with a one-line justification like the existing entries. Move the spike's pure logic (`keys.rs` key mapping, `term.rs` span building and colour resolution) under unit tests. Delete `spikes/native-client/` in the same commit. Add the new binary to CLAUDE.md "Project shape" and `rt.ps1` (a `native` verb that builds and runs it).
- [x] **P1.2 Shared daemon-client crate.** Move daemon supervision out of `apps/tauri-app/src-tauri/src/daemon_supervisor.rs` (ensure-running, health probe, protocol check, stale-binary detection, graceful `/shutdown`, orphan reaping, spawn lock) plus the handshake path, config-dir resolution and client identity (`lib.rs` `get_client_identity`) into a new `crates/daemon-client`. The Tauri host calls the crate, so there is still only one implementation. Behaviour-preserving: the existing supervisor tests move with the code.
- [x] **P1.3 Connection lifecycle** (parity: "Connection & daemon lifecycle"). Native client: ensure the daemon via `crates/daemon-client`; WS connect with `Hello` carrying `client_id` + hostname; reconnect with backoff (0.5s → 10s); standby-resume watchdog; connecting overlay with "Restart daemon"; footer status pill and troubleshooting flyout (open logs, restart, stop). Client identity (user, 2026-09-24): the native client keeps its own id, `daemon_client::client_identity("client-id-native")`, so its layout stays separate from the Tauri app's. Daemon reuse (user, 2026-09-24): the native client calls `ensure_running(RetirePolicy::ReuseCompatible)`, reusing any daemon whose protocol it speaks even from another build, so it never fights the Tauri app over a stale binary; Tauri keeps `RetireStale`. The client logs to `logs/native.log`. `LayoutInitRequired` is logged and left unanswered until P1.5 decides the reply.
- [x] **P1.4 App shell and sidebar** (parity: "Sidebar / repos / workspaces", the non-drag items). Main window with a resizable, collapsible sidebar: repos view (workspace / repo / SH / DIR containers, Detached bucket), session leaves with status dot, label, runtime tag and attention "!", click to focus. Sidebar state lives in a plain-Rust model with unit tests, and the GPUI view renders it. Rulings (user, 2026-09-24): sidebar width, the sidebar's collapsed flag and the set of collapsed containers persist in `native-ui.json` in the config dir. Until P1.5, clicking a leaf re-attaches the single terminal to that session (detach, load its scrollback, resize, clear "!"). Settled from the Tauri app: no Repos/Tabs toggle until P1.5, Ctrl+B and a button collapse the sidebar, and every session is shown, orphan and headless included. The first step pulls the terminal out of the root `TerminalView` into its own pane entity, with no change in behaviour, so P1.5–P1.7 mount into a separate root.
- [x] **P1.5 Tabs and split panes** (parity: "Tabs & panes / layout", minus drag-and-drop and pop-outs). Tab strip (new, activate, rename, close with confirm, middle-click close), split tree with draggable dividers, split / close pane, focused pane per tab, smart placement, layout persisted through the daemon's per-client tab messages. The split tree is a plain-Rust model with unit tests (port the cases in `apps/tauri-app/src/utils/grid.ts`). The daemon already owns and changes the tree (`crates/daemon/src/tabs.rs`), so the client model is a view over `GridNode` plus focus and placement. Rulings (user, 2026-09-24): until the Phase 4 chooser exists, `LayoutInitRequired` is answered with `InitLayout{AllSessions}`. Clicking a sidebar session that no pane shows places it in the active tab by Tauri's smart placement (`App.tsx` `paneTargetForSession`) and focuses it; a session a pane already shows jumps to that tab and pane. Settled from the Tauri app: the same session may be in two panes, and `Detach` is sent only when the last pane showing it closes; pane dividers reuse the sidebar's drag logic and send `SetPaneRatio` on mouse-up; tab rename uses a text input ported from gpui-0.2.2 `examples/input.rs` as a reusable module; the active tab persists in `native-ui.json`; an empty pane shows a muted "Empty pane" until P1.7 adds its spawn buttons.
- [x] **P1.6 Terminal pane, production grade** (parity: "Terminal", the non-(hard) items plus Shift+Enter and paste). Scrollback-first load with live buffering and 2s/4s retries; resize on layout change; mouse selection with copy (Ctrl+C copies with a selection, else ^C; Ctrl+Shift+C; copy on select); clipboard through GPUI's own API (user, 2026-09-24: no arboard) with paste on both Ctrl+V and Ctrl+Shift+V, bracketed when asked; terminal replies (`PtyWrite`: cursor-position and device-attribute answers) are forwarded as input (user, 2026-09-24). Settled from the Tauri app: copy on select is on by default, a constant until the settings modal exists; Shift+drag selects even when the child has asked for mouse reporting (the xterm convention); the scrollback retry mirrors `Terminal.tsx:684-796` (8s request timeout per `api.ts:373`, retries after 2s and 4s, the live buffer cleared after a retry, the status written into the terminal); Shift+Enter per agent (`\` + CR for claude and shells, `\n` for codex/cursor); cursor bar for agents / block for shells; input dropped when stopped; mouse reporting to the child when it asks; `detach` on close; focus after spawn. Two parts of this item land elsewhere: `detach` on pane close and resize on pane-layout change belong to P1.5, and focus after spawn belongs to P1.7. Still open from its parity lines: paste logging and the 5000-line scrollback cap.
- [x] **P1.7 Spawn and session actions** (parity: "Spawn dialog & launch flows" core + "Sessions"). Spawn dialog: target, runtime, open-in tab, interactive mode, trusted launch, worktree new / existing with the daemon-suggested branch name and base branch; + Shell (quick and dialog). Session context menu and pane header: rename, stop (keep / delete worktree through the branch-fate confirm), restart, remove pane; stopped-pane overlay. Split into P1.7a–d below (user, 2026-09-25). Rulings:
  - **Settled from the Tauri app and this item's text.** The delete-worktree confirm with branch fate belongs to P1.7 (P1.7b), not Phase 4. Restart sends `DuplicateSession` and discards the old session once the new one is placed (`App.tsx:1496-1532`); there is no restart message. Stop is a two-step inline confirm in the pane header and a sub-state in the menu. Rename is inline in a right-click context menu, and a blank name restores the default. The spawn dialog is a modal overlay: Esc closes it, a backdrop click doesn't, and a double submit is blocked. Ctrl+N opens it. Branch suggestions are cached per process.
  - **Rulings (user, 2026-09-25).** Dialog scope is core and interactive only: target (repos and workspaces), runtime with the last-spawn default, Open in, trusted launch, worktree new / existing, suggested branch with Random, base branch defaulting to `origin/<default>`, all plain text inputs. Headless, Advanced (model / env), spawn preview, collision radios and the branch combobox go to Phase 4. "Create a worktree" is offered, seeded from `default_use_worktree`, and turning it off wires the `CheckoutConfirmRequired` modal. Spawn replies are matched by a new optional `request_id` on `SpawnSession` and `DuplicateSession`, echoed on `SessionUpdated` (additive, no version bump, TS mirror updated). Errors: an auto-dismissing toast for `Error` (and "Spawning session…"), at most 3 visible, and a modal with title / detail / hint for `ActionFailed`. The quick + Shell opens in a directory remembered in `native-ui.json`, set by the Shell… dialog's "Use as quick shell default" (on by default), else `cwd: None`. Inactive sessions get Resume and Remove (± worktree) in P1.7a.
- [x] **P1.7a Session actions.** Pane-header two-step Stop and "exit code N", the stopped-pane overlay (Restart / remove pane keeping the worktree / remove pane ± worktree), and the session context menu on sidebar leaves and pane headers, by state: running → rename, Stop keeping or deleting the worktree; stopped → Restart, park, remove; inactive → Resume, remove. A stopped session with no pane is parked or discarded. "Delete worktree" routes to P1.7b's confirm. As built: the optional `request_id` landed here, since Restart is its first consumer. It is on `SpawnRequest`, `DuplicateSession`, `SessionUpdated`, `Error` and `ActionFailed`, and is echoed only on the reply to the requester, failures included. Abandoned sessions get their own group (Resume → `ResumeAbandoned`, Dismiss → `DiscardAbandoned`) with no exited overlay. Armed confirms are keyed by session and action, and disarm on any other press, Esc, a layout change or a reconnect. A pending Restart or Resume blocks a second click.
- [x] **P1.7a2 Protocol 23** (user, 2026-09-25, from the P1.7a review). An older daemon that still speaks protocol 22 accepts `request_id`, drops it and never echoes it, so a native Restart against it spawns the duplicate but never places it. Bump to 23: `protocol-version.json` `supported` becomes [23, 22], the native client offers 23 only (its own list, used by its `Hello` in `net.rs`), and the Tauri app stays on 22 (pin its vite-read constants). `daemon_client`'s compatibility check takes the client's list, so `ensure_running(ReuseCompatible)` retires a 22-only daemon through `/shutdown` and tracer sessions reattach, proved by a unit test over the decision. Starts once P1.10b lands (both edit `net.rs` and `daemon-client`), before P1.7c. As built: `daemon.json` held a single `protocol_version`, which can't say "speaks [23, 22]", so `DaemonHandshake` gained `supported_versions` (`#[serde(default)]`; a pre-23 file reads as its scalar). `ensure_running(policy, client_versions)` calls a daemon compatible when the two lists intersect (`protocol_kind`, unit-tested with `action_for`). The native client's list is `NATIVE_PROTOCOL_VERSIONS` in `net.rs`; the Tauri app pins 22 in `types.ts` literals and `TAURI_PROTOCOL_VERSIONS`, and its Vite config no longer serves the repo root, which only `protocol-version.json` needed.
- [x] **P1.7b Delete-worktree confirm with branch fate.** `PreviewDiscard` → `DiscardPreview` dialog (modes `delete_all` / `choose` / `worktree_only`, safe button focused first, 10 s fallback), then on confirm: `StopSession` if still live → `DiscardSession{remove_worktree, branch}`, and the daemon's discard closes the panes (as Tauri's menu and overlay do; no `ClosePane`). Wired into P1.7a's actions. As built: the choice applies to every member, as in Tauri (there is no per-member toggle); Enter and Space press the focused button, and Tab cycles through them; a late preview keeps the focus the user placed; the dialog closes when its session goes away or the connection drops.
- [x] **P1.10c Space key: verify, then fix** (found by the P1.10b smoke tier). gpui 0.2.2 gives VK_SPACE the key `"space"` with `key_char: None`. `keys::to_bytes` returns None for it, and the pane has no input handler for the `WM_CHAR` fallback, so a posted Space is dropped. A real keypress may take the same path; in-process specs miss it because a simulated space carries `key_char " "`. First check with a real keyboard (or trace gpui's real-keypress path). If Space is dropped, map `"space"` in `keys.rs` and add a spec using the Windows key shape; then switch the smoke spec's `echo.` workaround back to `echo `. Either way, decide whether the pane needs an input handler for IME and dead keys. As built: tracing gpui's Windows path showed a real Space is dropped too (`parse_immutable` maps VK_SPACE to `"space"` and skips `ToUnicode`, and the message loop never translates). `keys.rs` maps Space and Shift+Space to a space; Ctrl+Space still sends NUL. Rulings (user, 2026-09-25): Alt+Space stays unmapped so Windows opens the window's system menu, and Ctrl+Alt+Space stays unmapped. The input handler is P1.10e. Specs: `keys` unit tests, `ui_terminal::space_with_windows_key_shape_reaches_the_pty` (through a new `Harness::key_down` that skips gpui's simulated IME), and the smoke spec types `echo <marker>` again.
- [x] **P1.10e Pane input handler** (found by P1.10c). The pane has no `EntityInputHandler`, so text gpui delivers through `WM_CHAR` or IME never arrives: dead keys send `´e` instead of `é` (gpui passes the dead key's own char as `key_char`), AltGr characters probably send the base letter (gpui clears Ctrl and Alt under AltGr before `ToUnicode`), CJK IME composition is dropped, and `SendInput` `KEYEVENTF_UNICODE` text (on-screen keyboard, remote tools) is lost. Register an input handler on the pane (pattern: `text_input.rs`), send committed text as `Input`, handle marked text for composition, and stop `on_key` consuming plain text keys with no Ctrl or Alt so the `WM_CHAR`/IME path gets them. Rulings (user, 2026-09-25): the composition is drawn underlined at the cursor, which is also where the IME window sits, even when the program hides the cursor (claude does); Ctrl+Alt with a character that isn't an ASCII letter or digit types that character (AltGr on layouts gpui doesn't know), and Ctrl+Alt+letter or digit keeps ESC+char; a composition is dropped, with nothing sent, on blur or when the session stops; a dead key is not reported to gpui as a composition, because gpui would then hide Enter, Backspace, Esc and arrows from the pane. As built: after a dead key, Backspace withdraws the accent, Esc sends only ESC, Space sends only the accent, and any other consumed key sends the accent and then itself; each clears Windows' pending dead-key state with `ToUnicodeEx`. Letters now reach the pty through `WM_CHAR`, which Windows queues behind keys already posted, so the smoke spec pauses 25 ms between posted keys. Hand-test: a dead-key layout (´ Enter e gives `´`, CR, `e`), a German AltGr layout (`@`, `€`, `{` once each), and a Japanese IME inside claude (preedit and candidate window at the prompt, Enter commits without a CR).
- [x] **P1.10d Smoke pixel probes (research).** `PrintWindow(PW_RENDERFULLCONTENT)` read all black for an off-screen window, then a cloaked one, then a cloaked one with `GPUI_DISABLE_DIRECT_COMPOSITION` (0 non-black of about 120 captures a run). Those runs had a bug that has since been fixed: the Win32 show calls ran inside gpui's build closure ("RefCell already borrowed", a 2-column PTY). Retry from the threaded show, then try Windows.Graphics.Capture. The window must never take focus or cover the user's work (user, 2026-09-25). As built: from the threaded show, `PrintWindow(PW_RENDERFULLCONTENT)` reads the cloaked window's real pixels, with DirectComposition on or off (every pixel non-black; the sidebar, footer and pane colours match `PANEL_BG`, `BAR_BG` and the terminal background). Windows.Graphics.Capture gave the same pixels but wasn't kept. The smoke specs poll until the sidebar and footer show their colours. After typing, they require at least 1000 changed pixels in the pane, compared with a baseline taken once the click's redraw has settled. After each capture they check the window is still cloaked and not foreground. Posted keys are now 100 ms apart: at 25 ms, letters arrived out of order under heavy CPU load.
- [x] **P1.7c Spawn dialog.** The dialog as ruled above, sending the `request_id` that P1.7a added, placement by the Open-in choice and smart placement, focus after spawn, the toast and `ActionFailed` modal, and the `CheckoutConfirmRequired` modal. Entry points: Ctrl+N and a sidebar toolbar "+ Session". Split into A (notices and the spawn pipeline) and B (the dialog). Rulings (user, 2026-09-25): the dialog preselects the focused pane's repo or workspace, else the first workspace, else the first repo; Ctrl+Shift+N opens it from anywhere, terminals included, and plain Ctrl+N only outside terminals and inputs; Enter in a dialog field submits. Settled: toasts sit bottom-right above the footer, last 8 s, have a ×, and a fourth drops the oldest; trusted launch defaults to off and is not persisted; the dialog sends `FetchRepo` on open and re-lists branches on `RepoFetched`; "+ Session" is disabled with no repos ("Register a repo to spawn repo-tied sessions."); the dialog closes on disconnect; pending spawns clear on reconnect, with no timeout; the "Share this worktree?" confirm and a workspace's existing worktrees are in scope.
  - [x] **P1.7c-A Notices and spawn pipeline.** `RootView::spawn(request, OpenIn)` stamps a `request_id`, shows "Spawning session…" and places the reply in its tab by smart placement, or in a new tab, and focuses it (`spawns.rs`, `TabsModel::place_in`). Every `Error` is a "Daemon error" toast; `ActionFailed` opens a modal with an OK button; `CheckoutConfirmRequired` opens Cancel / Stash & switch / Carry changes, Cancel focused first. As built: the prompt carries no request id, so it resolves to the oldest pending in-place spawn with its repo and branch, and a prompt matching none closes without sending; the modals close on reconnect but toasts stay; ActionFailed draws over the checkout prompt.
  - [x] **P1.7c-B Spawn dialog.** The dialog itself on top of `RootView::spawn`: target, runtime with the last-spawn default, Open in, trusted launch, worktree new / existing (single repo `ListWorktrees`, workspace `InspectWorktreesRoot`), "Share this worktree?", suggested branch with Random, base branch, `SetRepoWorktreeDefault` / `SetWorkspaceWorktreeDefault` on submit, Ctrl+Shift+N / Ctrl+N and "+ Session" in the sidebar header. `text_input.rs` needs `set_text` and a change event. As built: the form is `spawn_form.rs`, the view `spawn_view.rs`; the dialog reacts only to the daemon messages it asked for, so streaming output doesn't redraw it. It departs from Tauri in four places: a branch you typed is never overwritten by a later `Branches` reply, `RepoFetched` re-lists branches for a workspace too, choosing Plain shell clears Trusted launch, and a detached existing worktree is named after its folder. Hand-test: "+ Session" opens on the focused pane's repo with a `wt/…` name; Ctrl+N in a shell still sends ^N; unticking "Create a worktree" is remembered; picking an in-use worktree asks "Share this worktree?".
  - [x] **P1.7c-C Queue checkout prompts** (found in P1.7c-A). A second `CheckoutConfirmRequired` replaces the one on screen, and the first prompt's spawn stays pending until reconnect. Queue prompts and show them one at a time. Ruling (user, 2026-09-25): the heading counts the queue as "(n of m)". As built: `CheckoutConfirmRequired` carries the spawn's `request_id` (additive; the daemon sends the prompt only to the requester), so a prompt resolves to its own spawn, and an id-less prompt from an older daemon falls back to repo and branch. At most one of these exists at a time: a shown prompt, or one outstanding re-ask. A queued prompt isn't shown from the numbers it arrived with. When its turn comes, its spawn is resent unchanged, because an earlier answer (Stash, or Carry onto the same branch) may have changed the repo. The daemon then either spawns it or sends a fresh prompt. A queued spawn that is placed or fails leaves the queue, and the counter resets once nothing is shown or waiting. A re-ask shows no toast.
- [x] **P1.7d Shells and empty-pane spawn.** Part 1 (shells) landed. Part 2 landed: a pane-aimed spawn replaces only the occupant the pane had when it was aimed, and otherwise lands in the aimed tab by smart placement; the overlay's "New session…" rebinds every pane showing the stopped session, then discards it (worktree kept); the no-tab empty state waits for the tab and repo lists. As built: "+ Shell" and "Shell…" sit in a toolbar row under the sidebar header, which wraps when the sidebar is narrow. The dialog takes only absolute paths, with a hint for anything else. The quick-shell folder is saved only once its spawn succeeds. "Use home folder for + Shell" forgets it. The spawning toast says "Shell startup may take a few seconds." for a shell. Part 2 is still open: pane-targeted spawns, the empty pane's "New session…" and "Shell here" (user, 2026-09-25), the overlay's "New session…", and the main-area empty state (user, 2026-09-25: in scope). Original text: Quick + Shell and the Shell… dialog (folder field, Browse through `prompt_for_paths`, which is hand-tested, "Use as quick shell default"), spawn buttons in the empty pane (the dialog preselects the neighbouring pane's target), and the overlay's "New session…".
- [x] **P1.10 UI test harness** (split into P1.10a and P1.10b below) (user, 2026-09-24: pulled forward from Phase 6's "UI test driver that replaces WebdriverIO" design item). A checked-in harness that drives the native client and asserts on what it does, so the P1.4/P1.6 hand-test lists become specs. Design round open. Option found: gpui-0.2.2's `test-support` feature provides `VisualTestContext` with `simulate_keystrokes` / `simulate_input` / `simulate_mouse_down|move|up` and a test clipboard (`src/app/test_context.rs:715-756`). That runs in-process and headless, and never takes over the user's mouse and keyboard. Rulings (user, 2026-09-24):
  - **Two layers.** Most specs run in-process in `VisualTestContext`, as part of `cargo test` and prek. They assert on grid text, the selection, the clipboard and the `ClientMessage`s sent. A thin OS-level smoke tier (a real window, `SendInput`, screenshots) covers what the test platform does not paint.
  - **Two backends.** UI specs run against a scripted fake daemon: inject `DaemonMessage`s, capture `ClientMessage`s. A smaller end-to-end tier runs against a real isolated daemon (config, binaries and worktrees dirs all under `.tmp/`), with plain shells and `tools/e2e/fake-claude`.
  - **Order.** This item starts as soon as P1.5 lands, before P1.7. Its first specs are the P1.4–P1.6 hand-test lists.
  - **Design (explored and ruled, user, 2026-09-24).**
    - `apps/native` gains a `src/lib.rs`, and `main.rs` becomes a thin binary. Specs go in `apps/native/tests/ui_*.rs`.
    - `RootView::with_transport(tx, events, ui_dir, paths, wanted, window, cx)` takes the `NetCommand`/`NetEvent` channel pair that `net::spawn` uses today. Specs never start the net thread or a daemon.
    - `ui_dir` is injected, so specs never write the real `native-ui.json`. The scrollback timer takes an injectable now-fn, so `advance_clock` drives it.
    - gpui `test-support` is a dev-dependency. It pulls in git2 → libgit2-sys, so the first step measures cold and warm `cargo test -p rustling-tulip-native` time with and without it. Under about 60s cold, it's accepted. Above that, UI specs go behind a `ui-tests` feature that prek pre-push enables.
    - The OS and end-to-end tiers are Rust tests marked `#[ignore]` and run by their own `rt.ps1` verbs. Screenshot checks probe pixels at computed cells, never golden images.
    - The test platform shapes text as fixed 0.6em glyphs (`NoopTextSystem`), so in-process specs assert on cells, never on real font pixels.
- [x] **P1.10a In-process UI specs.** The lib split, the `with_transport` / `ui_dir` / clock injection, test-support with the build-time measurement, a fake-daemon spec helper, and specs for the P1.4–P1.6 hand-test lists. P1.7 starts once this lands.
- [x] **P1.10b End-to-end and OS smoke tiers.** An isolated real daemon (`.tmp/` config, binaries and worktrees dirs, spawned with `Command::envs`, never process-wide env) with plain shells and `fake-claude`. A real-window tier driven through `SendInput` from the `windows` crate, with pixel probes. `rt.ps1 native-e2e` and `native-smoke` verbs. Rulings (user, 2026-09-25):
  - **E2E tier in-process.** `net::spawn` takes injectable ensure / identity / stop (`daemon_client` `*_in(config_dir)` variants), so specs reuse the `Harness` against a daemon the test spawns. Specs create sessions by sending `SpawnSession` through a clone of the client's `NetCommand` sender, never through a second WS client.
  - **Smoke tier as a child process.** Launch `CARGO_BIN_EXE_rustling-tulip-native` with isolated env on the child, find its HWND by pid with `EnumWindows`, drive it with `SendInput`. Pixels are read by `BitBlt` from the screen DC (GPUI draws through DirectComposition, so a window DC reads black) and probed for known UI colours and for a change in the pane after typing; the window is brought to the front first, and the test fails loudly if it isn't foreground.
  - **Superseded (user, 2026-09-25): the smoke window never takes focus or covers the user's work.** It opens off-screen and unactivated: the native client honours `RUSTLING_TULIP_OFFSCREEN_WINDOW`, as the Tauri app does for e2e. Input is posted to the HWND (`PostMessage` of key and mouse messages) instead of `SendInput`, and pixels are read with `PrintWindow(PW_RENDERFULLCONTENT)` instead of a screen `BitBlt`. If that reads black for a DirectComposition window, the test fails and says so. It never falls back to a foreground or on-screen window.
  - **Node.** A fake-claude spec with no `node` on PATH fails with "node not on PATH; fake-claude needs it", never skips.
  - **Smoke without pixels (user, 2026-09-25).** `RUSTLING_TULIP_OFFSCREEN_WINDOW` opens the window cloaked (`DWMWA_CLOAK`, from a `cloak-window` thread) and never activated. The smoke specs check that it is cloaked and not foreground, that `native.log` says "connected to daemon", and that posted keys reach the shell (read back through `LoadScrollback` over the test's own short-lived WS). Pixel probes moved to P1.10d.
  - **Found and fixed here.** ConPTY's startup `ESC[6n` can land in the scrollback a pane loads, and the pane never answered queries from history, so the shell stayed blank. A query that ends the loaded history is answered, unless buffered live output follows it (`term_view.rs`).
  - **As built.** Specs: `e2e_live.rs` (5, including `live_kill_leaves_no_tracer_behind`, which kills tracers by full image path under the spec's own binaries dir) and `smoke_window.rs` (2). Only one pane per session, its size driver, answers terminal queries, so a session shown in two panes gets each reply once.
- [x] **P1.9 Scrollback request id** (found in P1.4 review). `DaemonMessage::Scrollback` carries no request id, so after a quick A → B → A re-attach the pane can't tell which `LoadScrollback` a reply answers. It keeps the first reply per attach, which may be the stale one, with a brief gap or overlap against the live stream. Add an optional `request_id` to `LoadScrollback` and echo it on `Scrollback` (additive, `#[serde(default)]`), then match it in `term_view.rs`. As built: the grid mints a UUID for each request and each retry (`RetryGate::start` / `claim`), and every pane showing the session expects it. The daemon now aborts the old forwarder, waits for it to stop, queues `Scrollback`, and only then starts the new forwarder. The reply says so with `forwarder_restarted: true`, and the pane then discards the output it held back while loading, because that output came from the old forwarder and is already in the history. The file-read fallback leaves the flag false, and the held-back output is appended after the history. A reply to an earlier id is dropped, and so is a retry timer left over from before a reattach. An id-less reply (a daemon from before this change) keeps the old rule: the first reply is accepted and the buffer is appended. Known limit: under heavy output the snapshot can miss chunks the old forwarder delivered.
- [x] **P1.9b Loaded cursor query answered twice** (closed as working as intended, 2026-09-25: answering a query that ends the loaded history is what keeps a fresh shell from sitting blank (P1.10b); a double answer needs two clients attaching at the moment a shell starts, and only the daemon could tell the cases apart) (found in P1.9). A history that ends in `ESC[6n` is answered by the pane even when the live output after it shows another client already answered. The race predates P1.9: live output after the reply was never checked either. Decide whether a pane answers only queries it sees arrive live, and leaves the answer for a loaded query to the client that sized the PTY.
- [x] **P1.8 Quit flow** (parity: exit dialog items). Intercept window close; quit silently with no live sessions; exit dialog (keep running / stop keep worktrees / stop remove worktrees with per-session branch fate / abandon); wait for `shutdown_ack`. As built: parity with Tauri's labels and buttons; the stuck warning says 5 seconds, matching the timer. The branch-fate walk works from a snapshot of the sessions active when it started: a session that stops mid-walk is still discarded with its chosen fate, and one created mid-walk is left to the shutdown. The stops, discards and `Shutdown` go to the net thread as one command (`NetCommand::Shutdown { before, drain }`). Offline, nothing is sent and the dialog shows "Nothing was changed" instead of quitting. After a shutdown the client never reconnects or respawns the daemon. Closing the window cancels an open delete confirm, Shell… dialog or spawn dialog. Hand-test: X and Alt+F4, keep running, stop, remove with two worktree sessions, and Force quit against a suspended daemon.

## Later phases

Each is split into brief-sized items (like Phase 1) when it becomes the current focus, from its parity sections.

- [x] **Phase 2 — terminal depth:** split into P2.1–P2.10 below (2026-09-25). Parity: the "Terminal" section of the parity checklist, plus its "Settings & appearance" lines for the appearance editor and session accent. Dependencies: P2.2 → P2.3, P2.5 → P2.6, P2.8 → P2.9 → P2.10. Every other pair is independent unless their exploration finds a shared file. Concurrency ceiling 3.
  - [x] **P2.1 Terminal leftovers.** As built: also services the DEC 2026 synchronized-update timeout (150 ms on the pane clock), and history never replays a pending sync as live. Original text: The 5000-line scrollback cap, paste logging, UTF-8 split across output chunks (check whether alacritty's parser already carries a partial sequence; add a test either way), and refit plus `Resize` when the font changes.
  - [x] **P2.2 Link detection core.** As built: `links.rs` plus `Terminal::detect_links_near`. Row text drops wide-glyph spacers and carries a character-to-column map, so CJK paths open by their real names. The regex classes are ASCII-only, as in JS. A link cut by the ±64-row window cap is not offered. Original text: Port `utils/terminalLinks.ts` and its cases to a plain-Rust module over the grid's rows and wrap flags: URLs, and paths (absolute, UNC, relative, `./`, `../`) with `:line:col`; trim trailing punctuation; stitch wrapped rows, including TUI box borders (up to 4 joins within ±64 rows); offer shorter fallbacks when a path doesn't exist.
  - [x] **P2.3 Link hover and open.** As built: Ctrl+hover underlines only the link's own cells in any pane under the mouse; Ctrl+click wins over mouse reporting and opens once per click; opening runs off the UI thread. Rulings (user, 2026-09-25): a path Windows flags as dangerous (`AssocIsDangerous`) or with a script or executable extension asks "Run <name>?" (Cancel, Show in Explorer, Run); a UNC path, or a relative path under a UNC folder, opens only for a host behind a mapped drive or listed in `unc_hosts` in `native-ui.json` (read on each click, never written by the client). No process-folder fallback for relative links; VS Code missing falls back to the default app with a toast. Original text: Links are underlined only while Ctrl is held. Ctrl-click opens a URL in the browser. A path resolves against the session's cwd or worktree, then opens in VS Code with `-g` when it has a `:line`, else in the OS default handler. The remote-mode toast waits for Phase 6.
  - [x] **P2.4 OSC 52.** As built: only `c` (clipboard) stores reach the system clipboard, `p`/`s` are ignored (Windows has no primary selection); an empty store is ignored; reads are answered empty; stores in replayed history are dropped; the "✓ copied" chip shows for every copy in the app (selection, Ctrl+C, the footer flyout, OSC 52). Original text: Clipboard writes from a program reach the system clipboard and show the "copied" chip. Reads are answered empty.
  - [x] **P2.5 Shell integration.** As built: a pre-scanner with its own vte parser splits the PTY bytes at each 133/633 mark and 3J/RIS/alt-screen boundary; records anchor to absolute rows (alacritty's own cap set to 2×5000+rows, the logical 5000 enforced with `update_history`, evictions counted in `base`, columns reflowed by logical line from the cursor); a pending synchronized update is flushed at each mark; a D with no C or E since its A makes no record; replayed records carry no duration. Known limit: scrolls inside the screen (RI at the top, `CSI T`, IL/DL, a region with top > 0) move text without moving dots. Original text: Parse OSC 133 A/B/C/D and OSC 633 E for plain shells. Keep command records anchored to grid lines, surviving scroll, reflow and scrollback trim, capped at 1000. Draw a gutter dot per command (ok / fail / unknown), with an exit-code and duration tooltip.
  - [x] **P2.6 Gutter-dot menu.** As built: the menu opens at the dot's right edge. Its header is `exit N · dur`, and it offers Copy command (the 633;E text, else the prompt rows up to the output joined without breaks), Copy output only, Copy command and output, and Re-run. Output rows follow the real mark order: C and D record their column, so output starts on the C row when C sits at column 0 and ends above D. Re-run types the command without Enter into its pane and focuses it. A multi-line command goes as a bracketed paste. Re-run is disabled for an empty or evicted command, for a stopped session, for a command holding ESC or CR, and for a multi-line command without bracketed paste. The menu closes on Esc, an outside click, the wheel, a resize, the font keys, Ctrl+B, the quit ask and the connection overlay, and it blocks the spawn and Shell… dialogs. Original text: Exit code and duration; copy command / output / both; re-run, which types the command without Enter.
  - [x] **P2.7 Theme.** As built: `theme.rs`, checked field by field against the TS run under Node; a program's OSC 11 background and OSC 12 caret colour still win; `Terminal::set_background` rebuilds the theme (P2.10 must reapply it after `fresh_terminal`). Original text: The fixed palette, with ANSI colours contrast-adjusted against the background (port `utils/terminalTheme.ts`), a white caret and a blue selection.
  - [x] **P2.8 Fonts.** As built: `fonts.rs` plus `assets/fonts/`, 14 faces (4.4 MB); default Geist Mono 13 with the natural line height; a family resolves against the installed list before a `Font` is built, falling back to Geist Mono, Cascadia Mono, Consolas, then Courier New; the app font is `terminal_font` in `native-ui.json`, applied live through `set_app_font`. Original text: Bundle Geist Mono, Fira Code, JetBrains Mono and Cascadia Code (all OFL), list the system fonts, add a bold toggle and sizes 8–32, all applied live.
  - [x] **P2.9 Font size levels.** As built: tab override, then session, then container, then app; Ctrl+= / − step the session from its size without the tab override (a pending value covers presses before the echo); Ctrl+Shift+= / − step the tab from the resolved size, stored as `tab_font_sizes` in `native-ui.json` (saves debounced 500 ms, pruned against a loaded tab list); Ctrl+0 clears both; the shortcuts work with the terminal focused; right-clicking a tab opens a menu with the size and Increase / Decrease / Reset. Original text: App / repo / session size (Ctrl+= / − / 0) and tab size (Ctrl+Shift+= / −), stored where Tauri stores them.
  - [x] **P2.10 Appearance editor.** As built (P2.10b): `appearance_view.rs` and `settings_view.rs` follow the decided design below; Apply acts when the change moves the accent or the frame; a repo or workspace send builds on the last unechoed send for that container (`RootView.container_sends`, dropped when a `Repos` / `Workspaces` list stores it, cleared on `Welcome`; a refused send stays the base until then); an app size equal to the default and bold off report `built-in`; reset links are dimmed when the level does not set the field; the editor, Settings and the container menu close on reconnect, on the exit ask, and when their target goes. P2.10a landed: the model (`appearance.rs`: session, then container, then app, then built-in, each field with its source), the session menu's Accent submenu (presets, recent, Inherit, Back), the accent on the sidebar stripe, the pane's left edge and its focus ring, the grid painted in the resolved background (or a program's OSC 11) with an unset frame following it, sends merged through in-flight `request_id`s, and validation shared from `protocol::AppearanceOverrides::normalized`. Decided (orchestrator, 2026-09-25):
    - One modal serves the session, repo and workspace levels; the app level is the Appearance tab of a new Settings modal (Ctrl+, and a ⚙ in the sessions panel header).
    - Rows follow Tauri's order and labels: `Sidebar accent and pane frame` (sets accent and frame together), `Shell background`, font family, size as −/+ steppers, and `Render terminal text in bold`.
    - Each row has a hint `Resolved: <value> from <session|repo|workspace|app|built-in>`, with `current` for a colour set at this level.
    - Reset labels are `Inherit` / `Inherit app` / `Use built-in`.
    - Colours: preset swatches, recent swatches, and a hex field with a live swatch and Apply (enabled only on a valid, changed value; `Use #RRGGBB` shows while it is invalid). Apply feeds the one shared recent list of 12; presets don't.
    - The font list is a filter field over Bundled, System (names starting with `.` dropped) and Current `(saved)`.
    - Right-clicking a repo or workspace opens a minimal container menu with `Appearance…`, and the session menu gains `Appearance…` before `Accent ▸`.
    - Session sends go through the in-flight `request_id` path; repo and workspace sends overlay the container's stored overrides; the app level writes `native-ui.json`.
    Original text: At app, repo/workspace and session level: accent colour, shell background (presets, 12 recent, custom), font family, size and bold, each showing its resolved value and where it comes from. The session accent drives the sidebar stripe, pane frame and focus colour.
- [ ] **Phase 3 — source control:** split into P3.1–P3.10 below (2026-09-25). Parity: "Source control & diff", plus the activity-bar line in "Sidebar / repos / workspaces". The daemon already handles every git message (`server.rs` ~1840–2133). Rulings (orchestrator, 2026-09-25):
  - Status, badge counts and diffs are keyed by (repo, worktree path), fixing two Tauri bugs: its diff tab never sends `worktree_path`, and its badge lets worktree and main-tree counts overwrite each other.
  - Commit-detail files are clickable and open an `@ sha` diff tab.
  - Open in forge is disabled, with a tooltip, when `origin` isn't a recognised forge.
  - The stash section's collapse state persists.
  - The diff is side-by-side only, with F7 / Shift+F7 for next / previous change.
  - Status letters are the daemon's (`?` for untracked, as Tauri shows).
  - Concurrency: P3.4, P3.6, P3.7 and P3.9 can run in parallel once P3.3 lands. Revised 2026-09-26: P3.4a lands first; P3.4 and P3.7 then run in parallel; P3.6 follows P3.4 (it reuses P3.4's `GitWriteError` banner and toast, the section slots and `Sidebar::set_sc_collapsed`).
  - Git reads carry a `request_id` (P3.4a), so a failed status, stash list, log, commit detail or remote lookup is matched to its section and shown there instead of as a keyless "Daemon error" toast.
  - Stashes are one list per repo (git keeps `refs/stash` in the common dir): a `Stashes` reply for any of a repo's trees updates every section of that repo.
  - Drop stash asks first, in a confirm dialog (user, 2026-09-26).
  - History is collapsed by default while a session is focused and expanded when browsing a repo (Tauri's rule; user, 2026-09-26); the toggle persists per section.
  - Commit detail is a pane under the commit list with a saved, draggable divider per section (user, 2026-09-26).
  - Open in forge opens the section's branch page (`/tree/<branch>`, GitLab `/-/tree/`, Bitbucket `/src/`), falling back to the repo home with no branch (user, 2026-09-26).
  - [x] **P3.4a Git read request ids.** `RepoStatus`, `ListStashes`, `ListCommits`, `GetCommit` and `GetRemoteUrl` take an optional `request_id`, and a failed read replies `Error { message: "<read> failed: …", request_id }` to the requester only. Also fixed: commit-detail renames had `path` and `from_path` swapped.
  - [x] **P3.1 Diff view design and spike.** As built: `diff_model.rs` (one Patience run under a single 2 s deadline, which `similar` already backs with Myers; Modify pairing; lazy per-row word spans at a 0.5 ratio, skipped for a side over 1000 bytes; hunks; a trim-whitespace option whose spans leave out the removed indent; `line_endings_changed()` and `final_newline_changed()` for changes that make no hunk) and `diff_view.rs` (one `uniform_list`, a shared horizontal offset clamped so the longest line's end stays at the column's right edge, only the visible columns of a line shaped, F7 / Shift+F7 walking from the drawn rows when the current hunk is off screen, a hunk that fits centred on its middle row), plus `tests/ui_diff_view.rs` and `examples/diff_bench.rs` (`$env:RUSTLING_TULIP_OFFSCREEN_WINDOW=1; cargo run --release -p rustling-tulip-native --example diff_bench`). Measured in a real window (release, 5k lines, 500 hunks): model build 2.6 ms, window open to first drawn rows 58 ms, worst scroll draw 7.2 ms (mean 3.3 ms). Adding `similar` makes cargo re-pick nine `windows-sys` / `windows-core` users onto versions already in the lock; nothing new is downloaded. Decided: a custom GPUI element. `similar` computes line and inline word diffs on the client from the full texts `FileSnapshot` already sends. Aligned left/right rows render in one `uniform_list` (synced scroll comes free; hunk navigation is `scroll_to_item`). The spike measures a 5k-line file (diff time, first paint, scroll) and records the result here. Blocks P3.8.
  - [x] **P3.2 Source-control model.** As built (`source_control.rs`): `ScKey` (repo, worktree; a member whose worktree is its repo's path, compared case- and slash-insensitively, is the main tree), `sections`, `ScModel` (status and stashes per key, cleared on `Welcome`, pruned on a `Repos` snapshot; `missing`; `badge_total` counts distinct paths, untracked included), `build_tree`, in-memory folder collapse, and `UiState.source_control` (`pinned_repo`, `collapsed` per section part). Original text: Plain Rust: sections from the focused session's members or the picked repo, status and stash stores keyed by (repo, worktree), the badge total, the folded changes tree (port `utils/changesTree.ts`), and the collapse state in `native-ui.json`.
  - [x] **P3.3 Activity bar and panel shell.** As built: `activity_bar.rs`, `source_control_view.rs` and `assets.rs` (the icons compiled in); the rail replaces the collapsed strip and the header's «; its active bar and the badge use the app accent; with no repos the header shows only the title; the picker closes itself when its button goes away; seeding runs on `Welcome`, `Repos`, session changes, focus and pin changes, never on terminal output; a pin to a removed repo is dropped on the next `Repos`. Decided (orchestrator, 2026-09-25):
    - Rail and icons: a fixed 40px rail, always visible, replaces the collapsed strip (`sidebar-show`) and the header's `«`. The icons are SVGs from Tauri's `Icon.tsx`, served by an embedded gpui `AssetSource`.
    - Persistence: `UiState.activity` holds the active view.
    - Badge: distinct paths over the focused session's section keys, else over every repo's main tree.
    - Seeding: request `RepoStatus` for every wanted key not loaded and not already requested (a requested set on `ScModel`, cleared on `Welcome`).
    - Refresh: re-requests status without clearing it.
    - Repo picker: a menu-style popup, shown only with no members and at least 2 repos.
    - Context line: `<session> · N repos`, `<repo> · no active pane` (with Tauri's tooltip) or `<repo> · pinned`.
    - Section bodies: `loading…` / `working tree clean` / `N changed` until P3.4.
    - Settings gear: on the rail, pinned to its bottom (P2.10b put it in the sessions header first).
    Original text: A rail with Sessions / Source control and the change badge (99+); clicking the active item collapses the sidebar; the choice is persisted. A header with refresh and a repo picker ("Auto · follow active pane" or a pinned repo), the empty state, and status seeding. Needs P3.2.
  - [x] **P3.4 Changes tree and stage / unstage / discard.** As built:
    - `changes_view.rs`, `sc_writes.rs` (pending per tree, banners, the paths a row sends) and `discard_confirm.rs`.
    - Section carets persist.
    - Every discard goes through the confirm, as in Tauri.
    - A failed status shows `couldn't load status: …` in its section, with no toast; the request ids hold one entry per key.
    - The daemon answers a write whose status refresh fails with `GitWriteError`, so pending always clears.
    - Open items in the file menu come with P3.8.

    Original text: Staged / Changes buckets, per-file hover buttons, Stage all / Unstage all / Discard all, the right-click menu, the discard confirm listing the paths, the pending state, and `GitWriteError` as a banner plus a toast. Needs P3.3. Also: a `RepoStatus` that fails is answered with a keyless `Error`, so its key stays requested and its section stays on `loading…` until Refresh or a reconnect; settle that here.
  - [x] **P3.5a Multi-line `TextInput`.** As built: `TextInput::multi_line` with `with_rows(min, max)` (default 2 to 6), wrapped to the width, scrolling past the maximum. Enter inserts a newline and Ctrl+Enter submits. Up / Down keep the goal column, and Home / End work per visual row. Paste and `set_text` normalise CRLF. `set_read_only` works in both modes. Single-line inputs are unchanged, apart from drawing a stray line break as a space.
  - [ ] **P3.5b Commit box** (split from P3.5 on 2026-09-26; decided by the orchestrator):
    - It shows as in Tauri: when something is staged, a draft exists, or a commit is out.
    - The draft survives folding, activity switches and reconnects, and is dropped when its section leaves.
    - The input is 2 to 6 rows, read-only while committing. Esc gives the keyboard back.
    - A commit's `GitWriteError` gets the banner plus a toast, like every write.
    - When a commit fails, the daemon's error includes git's stdout if stderr is empty, and no longer echoes the args.

    Original P3.5 text: A multi-line input (extend `TextInput`), Ctrl+Enter, "Committing…", `CommitOk` clears it, and a dismissable error. Needs P3.4. Note: a commit that succeeds but whose status refresh fails gets `GitWriteError { operation: "commit", error: "status refresh failed: …" }` and then `CommitOk`. Treat the commit as done: clear the box, and show only the refresh banner.
  - [x] **P3.6 Stashes.** As built:
    - `stashes.rs` holds one list per repo, request ids and pending per repo; `stash_view.rs` renders it.
    - Each section has a Stashes part under its buckets, with a push input created on seed.
    - `pop` / `apply` / `drop`; drop asks in a confirm that closes with a `Stash list changed` toast if the list moves under it.
    - Every stash action checks the stash is still the one shown (id, subject and date) before sending.

    Original text: A collapsible section with a count, stash with an optional message, pop / apply / drop, and live updates from the `Stashes` broadcast. Needs P3.3.
  - [x] **P3.7 History, commit detail, open in forge.** As built:
    - `history.rs` holds the model: paging by offset with sha dedupe, request-id routing for the three reads, retiring stale reads, a detail cache per sha, the remote per repo, `branch_url`, and the split clamps.
    - `history_view.rs` holds the view: one History block per section under a saved changes / history split, the detail pane under the list behind its own saved divider, and the forge button with its three disabled tooltips.
    - Reads go out when the panel is shown, never from layout.
    - Commit-detail files send `OpenDiffTab` against the sha.

    Original text: 50 commits at a time with "load more", a tooltip (sha, author, date), a commit detail pane with clickable files, the resizable changes / history split, and the forge link. Needs P3.3.
  - [x] **P3.8 Diff tab.** As built:
    - The model is `diff_tab.rs` and the view is `diff_tab_view.rs`, one entity per diff tab.
    - The model is built on the background executor, and `DiffView::set_model` keeps the scroll position.
    - A tab activates on `DiffTabOpened` in either arrival order. A tab that is already active is refocused. Neither steals the keyboard from an open confirm or menu.
    - Worktree and staged tabs refresh live on `RepoStatus`; `@ sha` tabs never refresh.
    - Whitespace is a global saved toggle (`diff_include_whitespace`).
    - Binary, too-large and empty states each show their own note.
    - Changes-tree rows and the file menu's Open item open diffs.
    - The daemon answers a failed `OpenDiffTab` or `GetFileSnapshot` with the request's id.

    Original text: File clicks send `OpenDiffTab` with the worktree path. Diff tabs render the P3.1 view with the path, "worktree vs index" / "vs HEAD" / "@ sha", a whitespace toggle (saved), a change count, first / previous / next / last (wrapping), and loading and error states. Needs P3.1 and P3.4.
  - [x] **P3.9 Daemon diff hardening.** `16fd93b` dropped it by accident: it was committed from an older base and undid `312260c` byte for byte. Restored 2026-09-26. As built (312260c): a side is refused when binary (a NUL in the first 8000 bytes, or not UTF-8) or over 2 MiB, with the reason in `FileSnapshot.unavailable`; a committed blob's size is checked before it is read. Original text: `file_snapshot` detects binary files and caps size, using additive fields on `FileSnapshot`, so the diff tab can show "Binary file" or "File too large" instead of garbage. Independent.
  - [ ] **P3.8b Keep the session's sections while a diff tab is active.** Activating a diff tab leaves no session pane focused, so `sections()` falls back to the repos' main trees. The session's worktree sections disappear, and any file menu, discard confirm or drop confirm on them closes. Orchestrator ruling (2026-09-26): while a non-terminal tab is active, the panel keeps the last focused session's sections. Also add `run_confirm` to `keyboard_free` (`lib.rs`), so a pending diff activation can't take the keyboard from it.
  - [ ] **P3.10 Syntax highlighting in the diff.** syntect with `default-fancy` (no C), by the daemon's `language`; a separate item because of its binary-size cost. Needs P3.8.
- [ ] **Phase 4 — flows and settings:** preset launch wizard, worktree manager, cleanup-failed dialog, first-connect layout chooser, settings modal, OS notifications and attention, toasts beyond P1.7c's error and spawn toasts, the spawn dialog's headless mode, Advanced section, spawn preview, collision radios and branch combobox, undo shelf, workspace creator, VS Code workspace prompt, headless view.
- [ ] **Phase 5 — windows and drag-and-drop:** pane / tab / session pop-outs as windows of one process, pane drag-and-drop with edge overlays, sidebar and tab drag-to-reorder.
- [ ] **Phase 6 — remote and cutover:** connection picker, LAN pairing, pinned-TLS tunnel (reuse `src-tauri/src/remote.rs` as a library), autostart, remote file transfer (fetch a host file by Ctrl-click or a "Fetch file…" popup and open it locally; see [remote-file-transfer.md](./remote-file-transfer.md)). Design item: the UI test driver that replaces WebdriverIO. Cutover: the installer ships the native client; delete `apps/tauri-app`, the TS protocol mirror, the `protocol-mirror` prek hook, the `protocol-sync-checker` agent and the WebdriverIO suite; update CLAUDE.md.
